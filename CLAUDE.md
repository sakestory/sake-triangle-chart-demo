@./AGENTS.md

# Claude 固有差分

- データ同期は `sake-master-sync`、改修・公開準備は `shindan-deploy` を使用する。
- 公開後の検品は `shindan-inspector`。共通の承認境界はAGENTS.mdを優先する。
- ユーザー領域の診断フックはこのローカルcheckout用の補助検査であり、他の環境で同じ保護があるとは仮定しない。
