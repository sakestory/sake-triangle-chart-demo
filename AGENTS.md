# 日本酒三角チャート診断アプリ — 共通作業ガイド

## 作業と承認

- 通常の調査・下書き・整理・コード編集・ファイル作成・テスト・ローカル起動は、依頼済みの範囲で進める。同じ確認を繰り返さない。
- git commit、git push、削除、OS設定変更、送信・投稿・予約確定・決済・契約・値付け最終、秘密・認証・個人情報の外部送信は、対象と内容を短く示して実行直前に橋野さんへ確認する。
- バックアップや過去の「全て許可」は、この直前確認の代わりにならない。確認待ちの操作以外は進めてよい。
- パスワード入力・本人の承認ボタン・決済操作は本人が行う。freeeの既存取引の修正も直前確認する。

- 読み取り専用依頼では公開用ファイル取得・作成も依頼の範囲に従う。業務の正本は `/Users/hashinomotoki/hashino_os/`。必要な案件資料だけ読む。

## セッション開始時の確認（重要）
- 診断アプリのデータ更新には **hashino_os（正本ファイル置き場）** が必要。ローカルでは `/Users/hashinomotoki/hashino_os/` を参照する。以下のadd_repo手順はクラウドで正本にアクセスできないときだけ。
- セッションに hashino_os が紐づいていなければ、まず `add_repo` で sakestory/hashino_os の追加を試みる。
  承認が通らない環境（スマホ等）では、必要なファイル（.numbers / .xlsx）のアップロードを橋野さんに依頼する。
- 橋野さんへ：セッション作成時にリポジトリ選択で sake-triangle-chart-demo と hashino_os の
  **両方**を選ぶと、この手間がなくなります（環境設定に両方登録しておくのが確実）。

## このリポジトリ
- `index.html` … 診断アプリ本体（関連JSやデータの所在は現行ファイルで確認）
- `cat_01.png`〜`cat_27.png` … 27タイプの酒猫画像
- `index_.html` … 旧版の控え。**触らない**
- デプロイ：main に push されると GitHub Actions（deploy-lolipop.yml）が
  `index.html`＋`cat_*.png` を SFTP で https://shindan.sake-story.com/ へ自動アップロード

## 正本（元栓）のルール
- 銘柄座標の唯一の正本＝ **hashino_os リポジトリ内の「銘柄座標マスター vX.X」Numbersファイル**（最新版を使う。旧版は old/ にある）
- Excel/Numbers＝橋野さんの編集の入口。JSON・アプリ側は「写し」。**アプリ側の座標を勝手に直さない**（次の詰め替えで消える）
- 分類「店データ」の20銘柄は聖域（橋野さんの確定値）。指示なしに変更禁止
- 入手マークは「橋野さん修正欄」が入っていればそちらが正、空欄ならマーク列のまま

## 手順の所在

- 同期・文言更新の詳細は `/Users/hashinomotoki/.claude/skills/shindan-deploy/references/project-data-workflows.md` の該当節だけ読む。クラウドでこのファイルが使えない場合は、正本と現行コードから手順を確認し、未確認を報告する。
- 版・データの本数・ソース構造・公開対象は実ファイルで確認する。以下の過去の単一ファイル構成を現在の仕様と決めつけない。

## その他の決まりごと
- 店内おすすめ欄に NATL_SAKE の銘柄を混ぜない（混入事故防止）
- 文言の正本：27タイプの表示文言は「◎日本酒診断_文言一覧 vX.X.X」Excelが編集の入口。
  shindan_logic_master のマスターMDは経緯・下書きの保管場所（更新したら後日そちらにも反映）
- 診断ログは Google Apps Script → スプレッドシートに記録（`?test=1`・localhost は除外）
- コミットメッセージは日本語で「vX.Y.Z 内容」の形式
